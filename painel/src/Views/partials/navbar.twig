<nav class="layout-navbar container-xxl navbar-detached navbar navbar-expand-xl align-items-center bg-navbar-theme" id="layout-navbar">
  <!-- Mobile menu toggle -->
  <div class="layout-menu-toggle navbar-nav align-items-xl-center me-4 me-xl-0 d-xl-none">
    <a class="nav-item nav-link px-0 me-xl-6" href="javascript:void(0)">
      <i class="icon-base bx bx-menu icon-md"></i>
    </a>
  </div>

  <!-- Page Title (left side) -->
  <div class="d-flex align-items-center flex-grow-1 gap-2 me-4" id="navbar-page-context" style="min-width: 0; overflow: visible;">
    {% if page_icon %}
    <i class="icon-base bx {{ page_icon }} text-primary flex-shrink-0" style="font-size:1.35rem;"></i>
    {% endif %}
    {% if page_title %}
    <span class="fw-bold" style="font-size:1rem; white-space:nowrap;">{{ page_title }}</span>
    {% endif %}
  </div>

  <!-- Right: User avatar -->
  <div class="navbar-nav-right d-flex align-items-center justify-content-end" id="navbar-collapse">
    <ul class="navbar-nav flex-row align-items-center ms-auto">
      <!-- User -->
      <li class="nav-item navbar-dropdown dropdown-user dropdown">
        <a class="nav-link dropdown-toggle hide-arrow p-0" href="javascript:void(0);" data-bs-toggle="dropdown">
          <div class="avatar avatar-online">
            <img src="{{ ASSETS_URL }}/img/avatars/1.png" alt class="w-px-40 h-auto rounded-circle" />
          </div>
        </a>
        <ul class="dropdown-menu dropdown-menu-end">
          <li>
            <a class="dropdown-item" href="#">
              <div class="d-flex">
                <div class="flex-shrink-0 me-3">
                  <div class="avatar avatar-online">
                    <img src="{{ ASSETS_URL }}/img/avatars/1.png" alt class="w-px-40 h-auto rounded-circle" />
                  </div>
                </div>
                <div class="flex-grow-1">
                  <h6 class="mb-0 text-truncate" id="user-nav-name">Admin Santis</h6>
                  <small class="text-truncate text-muted">Webmaster</small>
                </div>
              </div>
            </a>
          </li>
          <li><div class="dropdown-divider my-1"></div></li>
          <li>
            <a class="dropdown-item" href="javascript:void(0);" onclick="logout()">
              <i class="icon-base bx bx-power-off icon-md me-3"></i><span>Sair</span>
            </a>
          </li>
        </ul>
      </li>
      <!--/ User -->
    </ul>
  </div>
</nav>

<script>
function logout() {
    localStorage.removeItem('santis_jwt');
    window.location.href = '/login';
}
</script>
